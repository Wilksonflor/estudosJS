// For tem uma sintaxe mais complexa
// O controle em linha é melhor

for (var i = 0; i <= 10; i++) {
    console.log(`Repetindo o for ${i}`)
}

var arr = [1, 2, 3, 4]

for (u = 0; u < arr.length; u++) { //lenght é quantos itens eu tenho no array, usado para fazer loops em array 
    console.log(arr[u])
}

for(var x = 0;x < 100; x *= 3){
    console.log(x)
}